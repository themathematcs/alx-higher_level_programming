#!/usr/bin/python3

def safe_print_list(my_list=[], x=0):
    length = len(my_list)

    while length <= my_list:
        for x in my_mylist:
            print(x)
            x = x +1
        length=length + 1
